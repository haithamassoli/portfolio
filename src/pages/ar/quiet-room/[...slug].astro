---
/* quiet-room, in Arabic. Thin on purpose: the only reason this file exists is to
   keep quiet-room's stylesheet off the other seven designs' pages. */
import Home from '../../../designs/quiet-room/Home.astro';
import Work from '../../../designs/quiet-room/Work.astro';
import Hire from '../../../designs/quiet-room/Hire.astro';
import Project from '../../../designs/quiet-room/Project.astro';
import { designPaths, resolve, type RouteProps } from '../../../designs/paths';

export const getStaticPaths = () => designPaths('quiet-room', 'ar');

const props = Astro.props as RouteProps;
const { design, project, next } = resolve(props);
const lang = props.lang;
---

{props.kind === 'home' && <Home design={design} lang={lang} />}
{props.kind === 'work' && <Work design={design} lang={lang} />}
{props.kind === 'hire' && <Hire design={design} lang={lang} />}
{
	props.kind === 'project' && project && next && (
		<Project design={design} lang={lang} project={project} next={next} />
	)
}
